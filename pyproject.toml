[project]
name = "agent"
version = "0.1.0"
description = "AI Agent Learning Project"
requires-python = ">=3.10"
authors = [
    {name = "Your Name", email = "your.email@example.com"}
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
